<style lang="less">
  page{
    background: #4FC5CF;
  }
  .page__hd{
    padding:40rpx;
    .love-title{
      margin-top:80rpx;
      color:#fff;
      font-size:54rpx;
      font-family:黑体;
      font-weight:bold;
    }
  }
  .page__bd{
    padding:40rpx;
    .love-summary{
      color:#fff;
      font-size:40rpx;
      font-family:幼圆;
      margin-bottom:260rpx;
    }
  }
  .btn-join{
    background:#4D7BD9;
    padding:6px;
    color:#fff;
    font-weight:bold;
    margin:30rpx 0;
  }
  .love-licence{
    color:#fff;
    font-size:32rpx;
    text-align:center;
  }
</style>

<template>
  <view class="page">
    <view class="page__hd">
      <view class="love-title">告别各种尬聊，为您匹配志趣相投的有缘人</view>
    </view>
    <view class="page__bd">
      <view class="love-summary">无颜是一套人工智能匹配算法，根据你的喜好和对方的喜好，每周为您推荐匹配度最高的有缘人。</view>
      <button class="btn-join" @tap="join">立即加入</button>
      <view class="love-licence">点击加入表示您同意我们的条款及隐私政策</view>
    </view>

    <view>
      <navigator url="/pages/love/papers_view" class="btn-join" style="text-align: center;padding: 10px; margin-top:150px">💗爱的抉择💗</navigator>
    </view>
  </view>
</template>

<script>
import wepy from 'wepy';
import { baseUrl } from '@/config';
import { loveSpaceId } from '@/config';
import pageRouter from '@/utils/pageRouter'

export default class LoveIndex extends wepy.page {
  config = {
    navigationBarTitleText: '无颜'
  };

  data = {
    baseUrl: baseUrl,
    space_id: null,
  };

  async onLoad (e) {
    this.space_id = e.space_id || loveSpaceId;
    console.log("this.space_id============", this.space_id);

    await this.$parent.login();

    if(!this.space_id){
      wx.showToast({
        title: "未配置工作区ID",
        icon: "none"
      });
    }
  }

  methods = {
    join(){
      pageRouter.navigateTo({url: `/pages/love/invite_code?space_id=${this.space_id}`});
    }
  };
}
</script>
