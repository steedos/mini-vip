<style lang="less">
  page{
    background: #fff;
  }
  .page__hd{
    padding: 20px;
  }
  .btn-join{
    background:#E8B866;
    border-radius:30px;
    padding:2px;
    font-size:40rpx;
    margin:auto;
    width:500rpx;
    color:#FFFCEC;
  }
  .top-bar{
    background:#353535;
    height:400px;
    border-radius:100%;
    width:600px;
    position:absolute;
    left:50%;
    margin-left:-300px;
    top:-320px;
  }
  .card {
    background-color: #FD3B5F;
    background-image:-webkit-linear-gradient(315deg, #FD3B5F, #FB644E 50%, #FB6E36);
    background-image:linear-gradient(135deg, #FD3B5F, #FB644E 50%, #FB6E36);
    height: 360rpx;
    margin-top: 20rpx;
    margin-bottom: 30rpx;
    margin-left: 20rpx;
    margin-right: 20rpx;
    border-radius: 20rpx;
    display: -webkit-box;
    display: -webkit-flex;
    display: flex;
    -webkit-box-orient: vertical;
    -webkit-box-direction: normal;
    -webkit-flex-direction: column;
            flex-direction: column;
    position: relative;
    margin-bottom: 0px;
    background-repeat: no-repeat;
    background-size: cover;
    .card-header{
      margin:20px;
      color:#fff;
    }
    .card-footer{
      position:absolute;
      bottom:20px;
      left:20px;
      color:#fff;

      .muted{
        margin-left:20px;
        font-size:0.8em;
        color:#ED2047;
      }
    }
  }
  .love-introduce{
    text-align:center;
    margin:20px;
    .love-introduce-icon{
      .slds-icon{
        margin:auto;
      }
    }
    .love-introduce-title{
      font-size:18px;
      margin-top:10px;
      font-family:幼圆;
      font-weight:bold;
    }
    .love-introduce-summary{
      font-size:16px;
      color:#777;
    }
  }
</style>

<template>
  <view class="page">
    <view class="top-bar"></view>
    <view class="page__hd">
      <view class="card">
        <view class="card-header">
          <view class="card-name">
            <text>无颜交友主义开创者</text>
          </view>
        </view>
        <view class="card-footer">
          <text>限时 免费</text>
          <text class="muted">原价￥200</text>
        </view>
      </view>
    </view>
    <view class="page__bd">
      <view class="love-introduce">
        <view class="love-introduce-icon">
          <view class="slds-icon-standard-skill-entity slds-icon"/>
        </view>
        <view class="love-introduce-title">
          无颜交友主义
        </view>
        <view class="love-introduce-summary">
          提倡无颜交友，告别刷脸时代
        </view>
      </view>
      <button class=" btn-join" @tap="join">立即加入</button>
    </view>

    <view>
      <navigator url="/pages/love/papers_view" class="btn-join" style="text-align: center;padding: 10px">💗爱的抉择💗</navigator>
    </view>
  </view>
</template>

<script>
import wepy from 'wepy';
import { baseUrl } from '@/config';
import { loveSpaceId } from '@/config';
import pageRouter from '@/utils/pageRouter'

export default class LoveIndex extends wepy.page {
  config = {
    navigationBarTitleText: '无颜'
  };

  data = {
    baseUrl: baseUrl,
    space_id: null,
  };

  async onLoad (e) {
    this.space_id = e.space_id || loveSpaceId;
    console.log("this.space_id============", this.space_id);

    await this.$parent.login();

    if(!this.space_id){
      wx.showToast({
        title: "未配置工作区ID",
        icon: "none"
      });
    }
  }

  methods = {
    join(){
      pageRouter.navigateTo({url: `/pages/love/invite_code?space_id=${this.space_id}`});
    }
  };
}
</script>
