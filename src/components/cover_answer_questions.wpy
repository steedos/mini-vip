  <style lang="less">
.page__hd {
  padding: 40rpx;
  .love-title {
    margin-top: 40rpx;
    color: #353535;
    font-size: 65rpx;
    font-weight: bold;
      text-align: center;
  }
}
.page__bd {
  padding: 40rpx;
  .love-summary {
    color: #353535;
    font-size: 45rpx;
    margin-bottom: 50rpx;
  }
  .love-attention{
    color: #999;
    font-size:15px;
    margin-bottom:50rpx;
  }
}
.btn-join {
  background: #5f6fee;
  padding: 6px;
  color: #fff;
  font-weight: bold;
  margin: 30rpx 0;
}
</style>

<template>
  <view class="page">
    <view class="page__hd">
      <view class="love-title">问卷匹配</view>
    </view>
    <view class="page__bd">
      <view class="love-summary">
        回答下面的问卷，我们的人工智能算法将根据你的答案，为你寻找匹配度最高的人。
      </view>
      <view  class="love-attention">你回答的越认真，“无颜”为你匹配的越精准。</view>
      <button class="btn-join" @tap="next">下一步</button>
    </view>
  </view>
</template>

<script>
import wepy from 'wepy';

export default class ComponentQuestions extends wepy.component {

  props = {
		spaceId: {
      type: String,
      default: ''
    },
	}

  methods = {
    next() {
      // const answeredUrl = encodeURIComponent(`/pages/love/shake?space_id=${this.spaceId}`);
      // wx.navigateTo({
      //   url: `/pages/love/papers_view?object_name=love_answer&space_id=${this.spaceId}&answered_url=${answeredUrl}&answered_url_type=switchTab`
      // });

      const answeredUrl = encodeURIComponent(`/pages/love/papers_summary?space_id=${this.spaceId}`);
      wx.navigateTo({
        url: `/pages/love/papers_view?object_name=love_answer&space_id=${this.spaceId}&answered_url=${answeredUrl}&answered_url_type=reLaunch`
      });
    }
  };
}
</script>
